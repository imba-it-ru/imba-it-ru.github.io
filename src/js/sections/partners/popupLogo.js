// export const popup = {
//   elem: UI.POPUP,
//   open(obj) {
//     const img = this.elem.querySelector('img');
//     const p = this.elem.querySelector('p');
    
//     img.src = obj.imgColor;
//     p.textContent = obj.name;
//     p.textContent = obj.text;

//   }
// }

const UI = {
  
}

function open() {

  const img = this.elem.querySelector('img');
  const p = this.elem.querySelector('p');
    
  img.src = obj.imgColor;
  p.textContent = obj.name;
  p.textContent = obj.text;
}